<template>
  <div class="container">
    <!-- 头部（与成就页面完全一致） -->
    <div class="header">
      <div class="back-btn" onclick="routeBack">
        <text class="back-icon">←</text>
      </div>
      <text class="title">支持开发</text>
      <div class="empty-btn"></div>
    </div>

    <!-- 主内容区 -->
    <div class="main-area">
      <!-- 支付方式选择（水平排列） -->
      <div class="payment-options">
        <div class="option wechat {{currentTab === 1 ? 'active' : ''}}" onclick="setTab(1)">
          <text>微信</text>
        </div>
        <div class="option alipay {{currentTab === 0 ? 'active' : ''}}" onclick="setTab(0)">
          <text>支付宝</text>
        </div>
      </div>

      <!-- 二维码区域（放大尺寸） -->
      <div class="qrcode-container">
        <image class="qrcode-image" if="{{currentTab === 0}}" src="../../common/assets/donationQRcode/alipay_qrcode.png">
        </image>
        <image class="qrcode-image" if="{{currentTab === 1}}" src="../../common/assets/donationQRcode/wechat_qrcode.png">
        </image>
      </div>

      <text class="thanks-text">感谢您的支持！</text>
    </div>
  </div>
</template>

<script>
import router from "@system.router"

export default {
  private: {
    currentTab: 1 // 默认微信
  },

  setTab(index) {
    this.currentTab = index
  },

  routeBack() {
    router.back()
  }
}
</script>

<style>
.container {
  flex-direction: column;
  align-items: center;
  width: 100%;
  height: 100%;
  min-height: 480px;
  background-color: #1a2e22;
}

/* 头部样式 */
.header {
  width: 84%;
  height: 80px;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20px;
  padding: 0 8%;
}

.back-btn {
  width: 70px;
  height: 40px;
  background-color: #53a47a;
  border: 2px solid #b5dbc7;
  justify-content: center;
  align-items: center;
  display: flex;
  border-radius: 14px;
}

.back-icon {
  font-size: 28px;
  color: #e8f3f5;
}

.title {
  font-size: 36px;
  color: #bfe1d8;
  font-weight: bold;
}

.empty-btn {
  width: 70px;
}

.main-area {
  width: 90%;
  flex-direction: column;
  align-items: center;
  padding: 15px 0;
}

/* 支付选项样式 */
.payment-options {
  flex-direction: row;
  width: 100%;
  justify-content: center;
  margin-bottom: 25px;
  padding: 0 5%;
}

.option {
  width: 140px;
  height: 60px;
  justify-content: center;
  align-items: center;
  margin: 0 10px;
  border-radius: 14px;
}

.option.wechat {
  background-color: #1aad19;
}

.option.alipay {
  background-color: #00a0e9;
}

.option text {
  font-size: 28px;
  color: #fff;
}

.option.active {
  background-color: #41b876;
  box-shadow: 0 4px 8px rgba(65, 184, 118, 0.4);
}

/* 二维码区域（放大尺寸） */
.qrcode-container {
  width: 300px;
  height: 300px;
  padding: 15px;
  background-color: rgba(232, 243, 245, 0.15);
  border-radius: 20px;
  margin-bottom: 20px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.qrcode-image {
  width: 280px;
  height: 280px;
  border: 1px solid #69ba83;
  border-radius: 12px;
}

.thanks-text {
  font-size: 28px;
  color: #b5dbc7;
  margin-top: 15px;
}
</style>